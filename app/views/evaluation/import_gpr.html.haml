%h1 Import GPR distribution data from the TAMU registrar

= form_tag({action: :upload_gpr}, multipart: true) do
  .form-group
    %label{ for: "term"} Term
    = text_field_tag "term", nil, placeholder: "e.g. 2015C", class: "form-control"
  .form-group
    %label{ for: "data_file"} Data File
    = file_field_tag 'data_file'
  .form-group
    = submit_tag 'Upload', class: "btn btn-primary"
